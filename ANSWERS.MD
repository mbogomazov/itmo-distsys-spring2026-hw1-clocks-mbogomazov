# Вопросы

## К заданию 1

> **a. Проанализируйте результаты эксперимента: оцените с точки зрения теории и проведенного эксперимента (experiment.png), в каком диапазоне может лежать
относительная ошибка синхронизации в алгоритме Кристиана?**

**Ответ:** $\varepsilon \in [0,\ 0{,}5)$ при допущении $R(t_1) \ge RTT$ (оговорка в конце пункта); в проведенном эксперименте $\varepsilon \in [0,\ \approx 0{,}489]$. Абсолютная ошибка всегда $|\Delta| \le RTT/2$. Вывод и границы — ниже.

**Модель и вывод формулы ошибки.** Пусть $t$ — реальное время (тики), а скорость хода (drift) у обоих часов равна 1:

- $L(t) = t + s_L$ — локальные часы;
- $R(t) = t + s_R$ — удаленные (эталон).

Задержка запроса $d_1$, задержка ответа $d_2$, запрос уходит в реальный момент $t_0$.

1. $T_0 = L(t_0)$ — время отправки по локальным часам.
2. Сервер читает свои часы в момент $t_0 + d_1$: $T_s = R(t_0 + d_1) = t_0 + d_1 + s_R$.
3. Ответ приходит в момент $t_1 = t_0 + d_1 + d_2$: $T_1 = L(t_1)$, значит, $RTT = T_1 - T_0 = d_1 + d_2$.
4. Алгоритм ставит локальные часы в $T_s + \frac{RTT}{2} = t_0 + d_1 + s_R + \frac{d_1 + d_2}{2}$.
5. На самом деле на сервере сейчас $R(t_1) = t_0 + d_1 + d_2 + s_R$.

Абсолютная ошибка:
$$\Delta = L_{new}(t_1) - R(t_1) = \frac{d_1 + d_2}{2} - d_2 = \frac{d_1 - d_2}{2}, \qquad |\Delta| = \frac{|d_1 - d_2|}{2} \le \frac{d_1 + d_2}{2} = \frac{RTT}{2}.$$

Ошибка равна нулю при симметричном канале ($d_1 = d_2$) и приближается к $RTT/2$, когда одна из задержек много больше другой.

Относительная ошибка:
$$\varepsilon = \frac{|\Delta|}{R(t_1)} = \frac{|d_1 - d_2|}{2\,R(t_1)}.$$

**Эксперимент.** В `run_experiment` параметры такие: $d_2 = D = 100$ мкс, $d_1 = rD$ (где $r$ — коэффициент асимметрии, $r \in [1, 1000]$), $s_R = 2000$ мкс, тики стартуют с 0 ($t_0 = 0$). Тогда $R(t_1) = s_R + rD + D$ и
$$\varepsilon(r) = \frac{D\,|r - 1|}{2\,(s_R + D(r + 1))} = \frac{|r - 1|}{42 + 2r}.$$

При $r \ge 1$ функция возрастает, и $\lim_{r \to \infty} \varepsilon(r) = \frac{1}{2}$. Строка «по формуле» — значения $\varepsilon(r)$, строка «в эксперименте» — вывод `run_single_point` с теми же параметрами (эти же точки рисует `experiment.png`, S-образная кривая на логарифмической оси):

| $r$ | 1 | 10 | 100 | 1000 |
|---|---|---|---|---|
| $\varepsilon$ по формуле | 0 | 0,145 | 0,409 | 0,489 |
| $\varepsilon$ в эксперименте | 0 | 0,145 | 0,409 | 0,489 |

Кривая выходит на плато: значения $1/2$ она не достигает ни при каком конечном $r$.

**Теоретическая граница («магическая константа») — 50%.** Раз $|d_1 - d_2| < d_1 + d_2 = RTT$ (обе задержки положительны), то
$$\varepsilon < \frac{RTT}{2\,R(t_1)}.$$

Если удаленные часы к концу синхронизации показывают не меньше, чем реально прошло времени с начала обмена, то $R(t_1) \ge RTT$, и $\varepsilon < \frac{1}{2}$. Так и есть в эксперименте: $R(t_1) = s_R + RTT$ при $s_R \ge 0$. Значение $50\%$ достигается только в пределе: $d_1/d_2 \to \infty$ и $s_R \to 0$.

При $r < 1$ (ответ медленнее запроса) в этой же постановке ошибка совсем мала: $\varepsilon = \frac{D(1 - r)}{2(s_R + D(1 + r))} < \frac{D}{2(s_R + D)} \approx 2{,}4\%$.

Оговорка: граница $1/2$ опирается на допущение $R(t_1) \ge RTT$. Если его убрать (например, удаленные часы показывают почти 0), относительная ошибка не ограничена сверху. Поэтому относительная ошибка плохо подходит как мера точности синхронизации (см. пункт b).

> **b. Верно ли, что из-за возможно большой относительной ошибки алгоритма его применение
на практике может быть затруднительно (высокая неточность)? Докажите или опровергните это
с помощью, например, аналитического вывода или контрпримера.**

**Неверно.** Большая относительная ошибка в эксперименте — артефакт выбора точки отсчета. Качество синхронизации определяет абсолютная ошибка.

1. **Абсолютная ошибка не зависит от показаний часов.** Из пункта a: $|\Delta| = \frac{|d_1 - d_2|}{2} \le \frac{RTT}{2}$. Эта оценка зависит только от сети. Кроме того, клиент сам измеряет $RTT$ и поэтому **знает гарантию точности** каждой синхронизации: истинное время сервера лежит в интервале $[T_s,\ T_s + RTT]$.

2. **Относительная ошибка зависит от произвольного «нуля» часов.** $\varepsilon \le \frac{RTT}{2\,R(t_1)} \xrightarrow[R \to \infty]{} 0$ при фиксированном $RTT$. В эксперименте удаленные часы показывают всего $\approx 2000 + RTT$ мкс, то есть величину того же порядка, что и сетевые задержки. Поэтому $\varepsilon$ и доходит почти до 50%.

3. **Контрпример с реальными числами.** Unix-время на 16 сентября 2026 года $\approx 1{,}79 \cdot 10^9$ с $\approx 1{,}79 \cdot 10^{15}$ мкс. Возьмем интернет-канал с $RTT = 100$ мс и полностью асимметричной задержкой (худший случай $|\Delta| = 50$ мс):
$$\varepsilon \le \frac{0{,}05\ \text{с}}{1{,}79 \cdot 10^9\ \text{с}} \approx 2{,}8 \cdot 10^{-11}.$$
Абсолютная ошибка та же, что и в эксперименте с таким каналом, а относительная на 10 порядков меньше. Значит, «высокая неточность из-за относительной ошибки» — ложный тезис.

**Реальные ограничения алгоритма** связаны с абсолютной ошибкой и с моделью:
- точность не лучше $\pm RTT/2$: в интернете это десятки миллисекунд, в LAN — доли миллисекунды;
- алгоритм предполагает симметричный канал, а маршруты туда и обратно могут различаться;
- задержки плавают (jitter), поэтому одного замера мало. На практике делают несколько запросов и берут ответ с минимальным $RTT$. Если известна минимальная задержка $d_{min}$, оценку можно уточнить до $\pm(RTT/2 - d_{min})$;
- сервер один: он становится единой точкой отказа, и ему приходится полностью доверять;
- `add_offset` может перевести время назад, то есть часы перестают быть монотонными. На практике время «подтягивают» плавно (slewing), меняя скорость хода.

Итог: алгоритм применим, если нужная точность больше $RTT/2$. Так работает, например, NTP (он развивает ту же идею).

> **c. Оцените ошибку синхронизации с сетевой задержкой при запросе d_1 и сетевой задержке при ответе d_2, а также найдите ее численное значение для d_1 = 40 ms, d_2 = 80 ms.** 

> *Примечание: Разницу в drift (то есть в тактовой частоте) между часами можно принять пренебрежимо малой.*

Используем вывод из пункта a (drift пренебрежимо мал, сервер отвечает мгновенно):

- в момент прихода ответа на сервере время $T_s + d_2$;
- клиент выставляет $T_s + \frac{RTT}{2} = T_s + \frac{d_1 + d_2}{2}$.

$$\Delta = \left(T_s + \frac{d_1 + d_2}{2}\right) - (T_s + d_2) = \frac{d_1 - d_2}{2}.$$

Численно для $d_1 = 40$ мс и $d_2 = 80$ мс:
$$\Delta = \frac{40 - 80}{2} = -20\ \text{мс}, \qquad |\Delta| = 20\ \text{мс}.$$

Локальные часы после синхронизации **отстают** от удаленных на 20 мс: алгоритм закладывает на обратный путь 60 мс, а ответ летел 80 мс.

Проверка с гарантией алгоритма: $RTT = 120$ мс, значит, $|\Delta| \le RTT/2 = 60$ мс, и $20 \le 60$. Проверили и моделированием на классах из задания (`LocalClock` + `ClockWithNetworkDelay` с задержками 40 000 и 80 000 тиков): разница `local - remote` равна $-20\,000$ мкс.

*Замечание:* если drift не пренебрежим (относительная разность частот $\rho$), добавится ошибка порядка $\rho \cdot RTT$. Например, при $\rho = 10^{-5}$ и $RTT = 120$ мс это всего 1,2 мкс.

## К заданию 2

> **a. Верно ли, что сортировка последовательности временных меток, соответствующих векторным часам, существует всегда? Покажите, как свести задачу к понятию топологической сортировки графа.**

**Да, для любого конечного набора векторных часов сортировка существует.**

**Что требуется.** Нужна перестановка $s_1, \dots, s_M$ входных меток, в которой нет «инверсий причинности»: $\forall i < j:\ \neg(s_j \to s_i)$. Здесь $a \to b \iff \forall k: a[k] \le b[k]\ \wedge\ \exists k: a[k] < b[k]$; отсутствующий ключ считаем нулем.

**Сведение к топологической сортировке.** Строим ориентированный граф $G$:
- вершины — входные метки (по одной на каждую позицию во входном списке);
- ребро $u \to v$ проводим, если метка $u$ happens-before метки $v$.

Топологическая сортировка графа — это такой порядок вершин, при котором каждое ребро идет слева направо. Это ровно то условие, которое требуется выше. Значит, искомые сортировки — в точности топологические сортировки $G$.

**Почему топологическая сортировка всегда существует.** Она существует тогда и только тогда, когда граф ацикличен (DAG).

1. Отношение $\to$ — строгий частичный порядок:
   - *иррефлексивно*: $a \to a$ невозможно, так как нет $k$ с $a[k] < a[k]$;
   - *транзитивно*: если $a \to b$ и $b \to c$, то $a[k] \le b[k] \le c[k]$ для всех $k$, а в той позиции, где $a[k] < b[k]$, получаем $a[k] < c[k]$; значит, $a \to c$;
   - *асимметрично*: $a \to b$ и $b \to a$ вместе дали бы $a \to a$ по транзитивности, что противоречит иррефлексивности. (Для строгого порядка это именно асимметричность; антисимметричность — свойство нестрогого порядка: $a \le b \wedge b \le a \Rightarrow a = b$.)
2. Предположим, в $G$ есть цикл $v_1 \to v_2 \to \dots \to v_k \to v_1$. По транзитивности получаем $v_1 \to v_1$, а это противоречит иррефлексивности. Значит, $G$ — DAG.
3. В любом конечном DAG есть вершина без входящих ребер. Иначе можно бесконечно идти назад по входящим ребрам, и в конечном графе какая-то вершина повторится — получится цикл. Ставим такую вершину первой, удаляем ее из графа (оставшийся граф по-прежнему DAG) и повторяем. По индукции получаем топологический порядок. Это и есть алгоритм Кана, реализованный в `partial_sort`.

Одинаковые метки ребром не соединяются (ни одна не строго меньше другой), поэтому их можно ставить в любом порядке.

> **b. Верно ли, что если сортировка последовательности временных меток, соответствующих векторным часам, существует, то она единственна? Является ли она в таком случае тотально упорядоченной? Сформулируйте и докажите (в две стороны) необходимое и достаточное условие единственности частичной сортировки.**

**Единственность — в общем случае неверно.** Пример из подсказки: $a = [1, 0, 0]$, $b = [1, 1, 1]$, $c = [0, 1, 0]$. Здесь $a \to b$, $c \to b$, а $a \parallel c$. Корректных сортировок две: $(a, c, b)$ и $(c, a, b)$.

**Тотальность.** Если сортировка единственна, то на этом наборе happens-before — **тотальный** (линейный) порядок: любые две метки сравнимы. Это следует из критерия ниже. Тогда сортировка совпадает, например, с сортировкой по сумме компонент: при $a \to b$ сумма строго растет. Обратно, если порядок тотальный, сортировка единственна. Если же сортировок несколько, в наборе есть конкурентные события, и порядок только частичный.

**Критерий.** Считаем метки попарно различными: в реальной системе разные события имеют разные векторные метки. Одинаковые метки неразличимы, их перестановка дает ту же последовательность, поэтому для них критерий применяем к множеству различных значений.

> Сортировка единственна $\iff$ любые две метки набора сравнимы ($\forall x \ne y:\ x \to y \vee y \to x$, то есть набор — цепь) $\iff$ в найденной сортировке для всех соседних элементов $s_i \to s_{i+1}$.

Последняя форма удобна на практике: единственность проверяем за $O(M \cdot N)$ по уже найденной сортировке.

*Лемма.* Если в корректной сортировке соседние $s_i$ и $s_{i+1}$ несравнимы, то после их обмена сортировка останется корректной. Действительно, взаимный порядок меняется только у этой пары, а она несравнима, то есть инверсии причинности не возникает.

*Достаточность ($\Leftarrow$).* Пусть любые две метки сравнимы. Возьмем произвольную корректную сортировку и элемент $x$. Для любого другого элемента $y$ верно одно из двух: либо $y \to x$, и тогда $y$ обязан стоять левее $x$, либо $x \to y$, и тогда правее. Значит, позиция $x$ равна $|\{y : y \to x\}| + 1$. Это число зависит только от набора, а не от сортировки. Позиции всех элементов определены однозначно, поэтому корректная сортировка одна.

*Необходимость ($\Rightarrow$), через контрапозицию.* Пусть есть несравнимые $x \parallel y$; покажем, что сортировок хотя бы две. Возьмем любую корректную сортировку $s_1, \dots, s_M$ (она существует по пункту a). Допустим, все соседние пары в ней сравнимы. Так как $s_{i+1} \to s_i$ запрещено корректностью, получаем $s_1 \to s_2 \to \dots \to s_M$. По транзитивности тогда сравнимы любые два элемента, в том числе $x$ и $y$. Это противоречие. Значит, есть соседняя несравнимая пара $s_i \parallel s_{i+1}$. По лемме их обмен дает другую корректную сортировку, и она отличается от исходной, потому что метки различны. Сортировка не единственна. $\blacksquare$

Эквивалентность «все пары сравнимы» $\iff$ «все соседние пары сравнимы» получаем по ходу: $\Rightarrow$ очевидно, $\Leftarrow$ следует из транзитивности.

Критерий проверили перебором всех перестановок на 500 случайных наборах: 3 процесса, $M$ от 1 до 7, компоненты часов от 0 до 3, наборы с повторяющимися метками включены.

> **c. Предложите (теоретическое описание и идею, код не требуется) алгоритм для подсчета количества всевозможных корректных сортировок последовательности векторных часов.**

Задача — посчитать число **линейных расширений** частичного порядка, то есть число топологических сортировок графа $G$ из пункта a. В общем случае она #P-полна (Brightwell & Winkler, 1991), поэтому полиномиального алгоритма ожидать не стоит. Точный экспоненциальный подход — **динамика по подмножествам (bitmask DP)**.

**Идея.** Строим сортировку слева направо. Какие элементы можно поставить дальше, зависит только от **множества** уже поставленных элементов, но не от их порядка. Следующим можно поставить $v$, если все его предшественники уже стоят.

1. Строим граф: для каждой вершины $v$ строим битовую маску предшественников $pred[v] = \{u : u \to v\}$. Это $O(M^2 N)$.
2. $dp[S]$ — число корректных способов поставить первыми ровно элементы множества $S$. Множество $S$ при этом замкнуто вниз: вместе с элементом в нем лежат все его предшественники.
3. База: $dp[\varnothing] = 1$.
4. Переход «вперед»: для каждого $S$ и каждого $v \notin S$ с $pred[v] \subseteq S$ выполняем $dp[S \cup \{v\}] \mathrel{+}= dp[S]$.
   Эквивалентная форма «назад»: $dp[S] = \sum_{v \in S,\ v \text{ максимален в } S} dp[S \setminus \{v\}]$.
5. Ответ: $dp[\{1..M\}]$.

Подмножества обходим в порядке возрастания маски: $S \cup \{v\} > S$, поэтому к моменту обработки маски все переходы в нее уже сделаны.

**Сложность:** $O(M^2 N + 2^M \cdot M)$ по времени и $O(2^M)$ по памяти. На практике это работает для $M \lesssim 20\text{–}25$.

**Проверка на примерах:**
- цепь дает 1 (ср. с пунктом b);
- антицепь дает $M!$;
- пример $a = [1, 0, 0]$, $b = [1, 1, 1]$, $c = [0, 1, 0]$: $dp[\{a\}] = dp[\{c\}] = 1$, $dp[\{a,c\}] = 2$, $dp[\{a,b,c\}] = 2$.

Если во входе есть одинаковые метки и нужно число **различных последовательностей значений**, ответ делим на $\prod_i k_i!$, где $k_i$ — кратности одинаковых меток. Одинаковые метки имеют одинаковые связи со всеми остальными, поэтому любая их перестановка внутри корректной сортировки тоже корректна. DP и поправку на кратности сверили с полным перебором перестановок на тех же 500 наборах ($M \le 7$).

**Ускорения в частных случаях.** Если граф распадается на независимые компоненты размеров $m_1, \dots, m_k$ (элементы разных компонент попарно несравнимы), то ответ равен $\frac{M!}{m_1! \cdots m_k!} \prod_i L_i$, где $L_i$ — число сортировок $i$-й компоненты.

> **d. Оцените временную сложность реализованного вами в задании алгоритма, принимая за параметр N количество процессов системы (соответственно, размер векторных часов), 
и за параметр M количество векторных часов на входе.**

> *Подсказка: оцените количество сравнений векторных часов в вашем алгоритме и умножьте
эту оценку на сложность сравнения пары векторных часов. Временной сложностью является
выражение вида O(...).*

Реализация (`tasks/partial_sort/vector_clock.py`) — алгоритм Кана.

1. **Построение графа.** Для каждой упорядоченной пары $(i, j)$, $i \ne j$, вызывается `happens_before`: всего $M(M-1)$ сравнений. Одно сравнение векторных часов — это объединение множеств ключей и два прохода по $N$ процессам, то есть $O(N)$. Итого $O(M^2 \cdot N)$.
2. **Сама сортировка.** Каждая вершина один раз попадает в кучу и один раз из нее извлекается: $O(M \log M)$. Каждое ребро просматривается один раз, а ребер не больше $M(M-1)/2$: $O(M^2)$.

Итого:
$$T(M, N) = O(M^2 N) + O(M^2) + O(M \log M) = O(M^2 \cdot N),$$
так как $M \log M \le M^2 \le M^2 N$. Память: $O(M^2)$ под списки смежности (плюс сами входные данные $O(MN)$).

Для сравнения: наивный вариант «на каждом шаге ищем элемент, который не имеет предшественников среди оставшихся» делает $O(M)$ шагов и на каждом шаге $O(M^2)$ сравнений, что дает $O(M^3 N)$.